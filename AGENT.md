# AGENT.md —— 给评委的速览

> 这份文件是「高位回撤观测智能体」的 Agent 形态说明。
> 想直接跑起来看结果：见 README.md 的四条命令。

---

## ⭐ 30 秒看懂：这个作品用了币安官方的什么

一句话：**用币安官方的 Skills Hub 和官方开源资源拿数据，用我们自己写的规则引擎出结论。**

具体是三件事：

1. **官方技能 `binance`** —— 用它驱动的官方命令行工具 `binance-cli`，
   按官方 SKILL.md 里记载的 `binance-cli request GET <url>` 用法直接调官方公开入口取数；
2. **官方开源数据仓库 `data.binance.vision`** —— 用它归档的月度日线 ZIP 做历史行情回溯；
3. **官方公开行情接口 `data-api.binance.vision`** —— 免密钥的实时快照 + 全历史月线，用来算真实 ATH。

而「回撤怎么排、成交量分布怎么分桶、支撑压力怎么定」这套算法，是**本项目自己写的**
（`core/signals.py`），官方并没有提供这类合成结论 —— 这部分才是作品的价值所在。

> **话术说明**：我们不说「基于官方 skill 开发」，因为那不准确。
> 准确的说法是 **数据层用官方资源，算法层和结论层自研** —— 后两层官方都没有现成的。

---

## 一、实际用到的官方资源（逐项核对）

| # | 官方资源 | 用在哪 | 可核验的证据（均为实测） |
|---|---|---|---|
| 1 | 官方技能 `binance`（币安 Skills Hub） | 经官方 CLI 的 `request` 子命令调官方公开入口 | `--skill` 通道实测：官方 CLI 返回 **3701 个交易对**，端到端 **5.0 秒** |
| 2 | 官方开源数据仓库 `data.binance.vision` | 月度日线归档 ZIP（`data/spot/monthly/klines/<交易对>/1d/`） | `--official` 通道实测：共请求 **96 个归档文件**，7 个币各命中 **12/12** 个月 |
| 3 | 官方公开行情接口 `data-api.binance.vision` | 全市场 24h 快照 + 月线（算 ATH）+ 日线（算成交分布） | `--live` 通道实测：全市场 **3701 个交易对**，20 币回撤榜 + 3 份成交分布，全程 **4.4 秒** |

**线上演示页面用的也是这套**：网页接口返回的 `dataSource` 字段实测为
「币安官方公开行情接口」，BTC 的 365 根日线、18 个价格桶、支撑位/压力位全部正常返回。

---

## 二、官方 Skills Hub 全量核对：为什么只用这几项

币安官方技能市场（`binance/binance-skills-hub`）共 **20 个技能**，分两组。
我们逐个核对过**能不能真跑**，不做挂名：

### binance 组（7 个）

| 技能 | 结论 |
|---|---|
| `binance` | ✅ **在用** —— 本作品经它驱动的官方 CLI 取数 |
| `academy-skill` / `fiat` / `square-post` | ❌ 走 `www.binance.com` 域，实测连接被隧道拒绝（502） |
| `p2p` | ❌ 走 `c2c.binance.com`，不可达 |
| `payment` / `onchain-pay` | ❌ 域名可连但**必须自备 API 密钥并签名**，本作品是纯公开数据作品，不需要 |

### binance-web3 组（13 个）

这组里带命令行脚本的技能，主力接口都在 `web3.binance.com` 下，实测是
**SNI 层阻断**（用公共 DoH 取到真实 IP 后直连，正确 SNI 会被 40~50 毫秒内 `ECONNRESET`），
换 DNS、改 hosts 都救不回来。

**但要说准确**：不是整组都不可用 —— `query-token-info` 的 kline 子命令走的是另一个域
（`dquery.sintral.io`），实测是可达的。我们逐个命令核对过，结论是：
**这 13 个技能里，只有那一个子命令能跑通，但它提供的是链上 K 线，与本作品的中心化现货口径不匹配，
所以没有接入。** 没有采用「整组不可达」这种一刀切的说法。

其余未接入的原因也逐项交代：

- `crypto-market-rank`：它本身也是「全市场热度/涨幅/聪明钱榜」。**注意口径差别** ——
  它是链上 + 社交口径，本作品是中心化合约/现货的量价口径，两者互补而非重复。
  它走 `web3.binance.com`，实测不可达，所以没有接入。
- `binance-leaderboard` / `binance-wallet-tracker`：依赖 `binance-agentic-wallet` 的 MCP，走 web3 域，不可达。
- 其余（`meme-rush`、`query-token-audit`、`query-address-info`、`trading-signal`、
  `binance-trading-signal`、`binance-onchain-copy-trader`、`binance-tokenized-securities-info`、
  `binance-sports-ai-analyzer`、`binance-agentic-wallet`）：业务领域与本作品不同，或接口不可达。

### 关于官方 connector 仓库

币安官方还提供 connector 类仓库（各语言 SDK 连接器）。本作品**没有引入**，原因是：

1. 它们面向「用 API 密钥交易账户」的场景，本作品不涉及账户、不下单、不碰资金；
2. 它们默认指向 `api.binance.com` / `fapi.binance.com`，本机网络环境实测不可达
   （连接被隧道拒绝），引入会变成一个取不到数的死依赖；
3. 本作品的定位是**公开数据的观测工具**，用公开数据域就够了。

---

## 三、价值边界：哪些是官方的，哪些是自研的（三层法）

| 层 | 内容 | 来源 |
|---|---|---|
| **数据层** | 全市场快照、月线、日线、历史归档 | **币安官方**（能拿官方的一律拿官方） |
| **算法层** | 回撤公式、成交量分布的 18 桶分箱、支撑压力位选取、峰值区定位 | **自研**（`core/signals.py`，纯规则、可复现） |
| **结论层** | 回撤榜单排序、深度回撤标记、支撑压力对照结论 | **自研**（官方没有这类组合结论） |

要特别说清楚的一点：**是「官方没有这套算法和结论」，不是「官方没有数据」**。
币安官方的数据覆盖非常广（全市场行情、历史归档、合约指标等），
本作品的价值在于**把官方数据加工成可读的观测结论**。

---

## 四、四个数据通道

| 通道 | 数据来源 | 口径 | 实测耗时（20 币规模） |
|---|---|---|---|
| `--live`（默认） | 币安官方公开行情接口 | 全历史 ATH | 4.4 秒 |
| `--skill` | 官方技能 `binance` + 官方 CLI | 全历史 ATH | 5.0 秒 |
| `--official` | 官方开源数据仓库 | 回溯窗口内最高价 | 4.2 秒 |
| `--global` | 综合平台全市场口径（**非官方**） | 全市场综合口径 | 对照用 |

```bash
python agent.py                       # 默认通道
python agent.py --skill --json        # 官方技能 + 官方 CLI
python agent.py --official --months 24 --top 12
python agent.py --symbols BTC,ETH,SOL --depth 3
python agent.py "用官方开源数据看看 BTC 距最高还有多远"   # 自然语言，自动选通道
```

除了四个通道参数，还支持**一句自然语言**直接描述需求：程序会从话里识别
「官方技能 / 官方开源 / 官方行情 / 综合平台」等关键词自动选通道；
识别不出来就沿用默认通道，**显式参数永远优先**。识别不到时不会乱猜币种或参数。

`--json` 时 stdout 是纯 JSON，进度信息全部走 stderr，可以被别的程序直接消费。

> 补充说明：本作品是**横截面快照分析**，每次运行都从官方数据现算，**没有需要跨次保留的本地状态**，
> 因此不提供 `--status` / `--reset` 这类状态管理参数（同系列其他作品有，是因为它们要在本地累积逐笔样本）。

---

## 五、诚实边界（必须一起看的说明）

1. **ATH 是币安现货口径**，基于官方月线的最高价列。月线最高价就是该月最高价，所以这个 ATH 是精确值；
   但它与「全市场综合口径」的历史最高价可能略有差异，两者不可混用。
2. **`--official` 通道的 ATH 不是全历史 ATH**，只是回溯窗口（默认 24 个月）内的最高价。
   该通道下**现价有可能高于窗口最高价**，此时回撤是正数（表示创出窗口新高），这是正确行为，不是 bug。
   要全历史 ATH 请用 `--live` 或 `--skill`。
3. **官方开源数据仓库是 T+1 归档**，最新一两天通常还没发布。缺失的月份会在输出的
   `monthsMissing` 里如实标出，**不补齐、不猜数**。实测碰到过上新较快的币在归档里完全没有文件
   （12 个月全部缺失），此时该币的 `drawdown` 直接留 `None` 并排在榜单最后，绝不填 0 冒充。
4. **成交量分布是按收盘价归桶的粗粒度近似** —— 把一整天的成交额都算到收盘价所在的桶里。
   这是本引擎的既定口径，不冒充精确的逐笔成交量分布。
5. **现货快照不含合约维度**（持仓量、资金费率、多空比），本作品不使用这些指标。
6. **结果仅用于研究，不构成投资建议。**

---

## 六、与已提交参赛说明的关系

本项目已按「网页作品」的形式提交过一版说明。这次新增的是 **Agent / 技能形态**：

- **网页形态**（`app.py` + `dashboard/index.html`）继续保留，只是数据源换成了币安官方优先；
- **新增加的**是命令行入口 `agent.py` 与技能定义 `skills/gaowei-huiche/SKILL.md`，
  让这个作品可以被技能安装器装载、被其他 Agent 直接调用；
- 两种形态**共用同一套自研引擎**（`core/signals.py`），结论口径完全一致，不是两个不同的东西。
